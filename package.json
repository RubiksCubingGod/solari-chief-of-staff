{
  "name": "chief-of-staff",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@11.18.0",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "lint": "eslint .",
    "typecheck": "tsc --build --force && tsc -p tsconfig.test.json",
    "test": "node scripts/vitest.mjs run --coverage",
    "test:unit": "node scripts/vitest.mjs run --project unit",
    "test:integration": "node scripts/vitest.mjs run --project integration",
    "build": "tsc --build",
    "migrate": "node scripts/migrate.mjs",
    "clean": "tsc --build --clean",
    "check": "node scripts/check.mjs"
  },
  "devDependencies": {
    "@eslint/js": "10.0.1",
    "@types/node": "26.4.0",
    "@vitest/coverage-v8": "4.1.11",
    "eslint": "10.9.1",
    "globals": "17.11.0",
    "typescript": "6.0.3",
    "typescript-eslint": "8.69.0",
    "vitest": "4.1.11",
    "yaml": "^2.9.0"
  }
}
