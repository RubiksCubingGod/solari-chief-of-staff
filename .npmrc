# Workspace packages are linked from source, never fetched from the registry.
link-workspace-packages=true
# Keep transitive deps out of the top level so imports match declared dependencies.
hoist=false
strict-peer-dependencies=false
