{
  "name": "@chief-of-staff/playbooks",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Per-site deterministic scripted flows, and the guardrails every one of them runs inside.",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc --build"
  },
  "dependencies": {
    "@chief-of-staff/core": "workspace:*",
    "@chief-of-staff/db": "workspace:*",
    "@chief-of-staff/solari": "workspace:*",
    "playwright": "1.62.1"
  },
  "devDependencies": {
    "@chief-of-staff/fixtures": "workspace:*"
  }
}
