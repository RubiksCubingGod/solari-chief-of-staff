.nah/config.local.yaml
.nah/credentials.env
.nah/.traces/
node_modules/
dist/
coverage/
# The nested run that proves the coverage gate keeps its own report dir.
tests/fixtures/coverage-gate/.coverage/
*.tsbuildinfo
# The dashboard's build output, and the ambient types Next rewrites into the
# package on every dev and build run.
.next/
next-env.d.ts
.env
.env.local
