# Fixture sites

Placeholder. The fixture Express apps described in ARCHITECTURE §9.1
(`fakestore`, `fakegym`, `fakedmv`, `fakenews`) are built by the
`fixture-harness` sprint; this directory only reserves their home so the
repo layout matches ARCHITECTURE §11 from the start.
