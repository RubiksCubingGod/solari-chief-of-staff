schema_version: 1
id: action-playbooks
alias: s5
wall_hours_estimate: 21
estimated_task_count: 6
average_task_agent_hours: 3
created_at: 2026-09-01
created_by: RubiksCubingGod
owner: RubiksCubingGod
repositories:
  - id: chief-of-staff
    remote: RubiksCubingGod/chief-of-staff
release:
  outcome: The action engine in playbook mode - persisted task state machine with
    a waiting_user gate behind a UserIO port (scripted IO in proofs), fakegym
    cancellation playbook end-to-end, session recording stored on the task,
    code-enforced guardrails (domain allowlist, payment gate), and the dashboard
    task detail with replay embed.
  kind: feature
  change_sets:
    - repository: chief-of-staff
      depends_on: []
project_management:
  links: []
origin:
  summary: The action engine in playbook mode - persisted task state machine with
    a waiting_user gate behind a UserIO port (scripted IO in proofs), fakegym
    cancellation playbook end-to-end, session recording stored on the task,
    code-enforced guardrails (domain allowlist, payment gate), and the dashboard
    task detail with replay embed.
  refs:
    - .nah/projects/chief-of-staff/project.yaml
